<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Yugo - Plan your perfect group trip" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Open Graph meta tags for social sharing %>
    <meta property="og:title" content="<%= content_for(:og_title) || "Yugo - Plan your perfect group trip" %>">
    <meta property="og:description" content="<%= content_for(:og_description) || "Collaborate with friends to plan amazing group trips. Share preferences, vote on activities, and create the perfect itinerary together." %>">
    <meta property="og:type" content="<%= content_for(:og_type) || "website" %>">
    <meta property="og:url" content="<%= content_for(:og_url) || request.original_url %>">
    <meta property="og:image" content="<%= content_for(:og_image) || image_url('paris-trip.jpg') %>">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <%# Mapbox GL JS %>
    <script src='https://api.mapbox.com/mapbox-gl-js/v3.3.0/mapbox-gl.js'></script>
    <link href='https://api.mapbox.com/mapbox-gl-js/v3.3.0/mapbox-gl.css' rel='stylesheet' />
  </head>

  <body>
    <header>
      <%= render "shared/navbar" %>
      <%= render "shared/flashes" %>
    </header>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
