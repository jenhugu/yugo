<turbo-frame id="form_wrapper">
  <h1>Your preferences are saved 🎉</h1>

  <p>Budget: <%= @preferences_form.budget %></p>
  <p>Travel pace: <%= @preferences_form.travel_pace %></p>
  <p>Steps per day: <%= @preferences_form.steps_per_day %></p>

  <p>Activities: <%= @preferences_form.activity_types.join(", ") %></p>

  <p>Your interests:</p>
  <ul>
    <% @preferences_form.interests.each do |interest, value| %>
      <li><%= interest.to_s.humanize %>: <%= value %>%</li>
    <% end %>
  </ul>

  <%= link_to "Start over", new_trip_preferences_form_path(@preferences_form.user_trip_status.trip), data: { turbo: false } %>
</turbo-frame>
